import React from "react";
import Layout from "../components/Layout";

const Blog = () => {
    return (
        <Layout>
            <h1>Blog</h1>
            <p>No posts yet</p>
        </Layout>
    )
};

export default Blog;
